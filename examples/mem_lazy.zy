import <std/mem>;

fn main() -> int {
    let p = ptr<int>;

    set *p = 7;
    print((str)(*p));
    print((str)(mem.is_owned(p)));

    mem.free(p);
    print((str)(mem.is_valid(p)));

    return 0;
}
