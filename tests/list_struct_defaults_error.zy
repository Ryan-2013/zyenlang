struct DefaultDog {
    fn speak(times: int = 1) -> void {
        print("dog");
    }
}

struct DefaultCat {
    fn speak(times: int = 2) -> void {
        print("cat");
    }
}

fn main() -> int {
    let values: List = [DefaultDog {}, DefaultCat {}];
    values.get(0).speak();
    return 0;
}
