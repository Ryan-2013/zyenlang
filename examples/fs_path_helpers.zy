import <std/fs>;

fn main() -> int {
    let p: str = fs.join("src", "main.zy");
    print((str)(p));
    print((str)(fs.basename("src/main.zy")));
    print((str)(fs.dirname("src/main.zy")));
    print((str)(fs.extension("src/main.zy")));
    return 0;
}
