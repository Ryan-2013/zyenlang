struct SignatureDog {
    fn bark() -> void {
        print("dog");
    }
}

struct SignatureCat {
    fn bark(times: int) -> void {
        print("cat");
    }
}

fn main() -> int {
    let values: List = [SignatureDog {}, SignatureCat {}];
    values.get(0).bark();
    return 0;
}
