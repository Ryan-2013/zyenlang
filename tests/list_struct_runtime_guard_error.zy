struct GuardDog {
    fn bark() -> void {
        print("dog");
    }
}

fn bark_first(values: List) -> void {
    values.get(0).bark();
}

fn main() -> int {
    let values: List = [1];
    bark_first(values);
    return 0;
}
