import <std/list>;

fn main() -> int {
    let xs: List;
    xs.append("robot");
    xs.append("cv");
    xs.append("control");
    print((str)(list.join_str(xs, " | ")));
    let rev: List = list.reverse(xs);
    print((str)(list.join_str(rev, " -> ")));
    return 0;
}
