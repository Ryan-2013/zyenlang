import <std/request>;

fn main() -> int {
    let response: Response = request.get("https://example.com", 10000);
    if (response.ok()) {
        print(response.body);
    } else {
        print(response.error);
    }
    return 0;
}
