import <std/cmd>;

fn main() -> int {
    let code: int = cmd.run("echo ZyenLang command module");
    let ok: bool = cmd.success(code);

    print((str)(code));
    print((str)(ok));

    return 0;
}
