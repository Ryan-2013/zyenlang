struct MissingDog {
    fn bark() -> void {
        print("dog");
    }
}

struct MissingRock {
}

fn main() -> int {
    let values: List = [MissingDog {}, MissingRock {}];
    values.get(0).bark();
    return 0;
}
